LANScape - A python based local network scanner.

This project is a portable network scanning tool built in Python & React. The current repo is the main codebase for the Python scanning library. It interfaces with the React UI via a WebSocket server, sending real-time updates on discovered devices, open ports, and identified services.

Check the instructions folder and consume instructions based on which agent you are. 

